# Day 13 Summary (16-07-2026)

* Learned how to install **Docker Desktop** on Windows.
* Understood the prerequisites and setup process for running Docker on a Windows system.
* Explored **Docker Hub** and learned how to search for and download official Docker images.
* Pulled the **Ubuntu** image from Docker Hub using Docker commands.
* Created and ran an **Ubuntu Docker container**.
* Learned how to access and interact with a running Ubuntu container using the terminal.
* Practiced viewing all running and stopped Docker containers using Docker commands.
* Learned how to **restart an existing container** instead of creating a new one every time.
* Assigned a **custom name** to a Docker container for easier identification and management.
* Practiced stopping and removing Docker containers when they were no longer required.
* Installed software inside the Ubuntu container using package management commands (e.g., `apt update` and `apt install`).
* Gained hands-on experience with the complete Docker container lifecycle, from installation to container management.
* Developed a practical understanding of Docker fundamentals and container-based application environments.
