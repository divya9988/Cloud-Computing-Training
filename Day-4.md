Day 4 - Task 3 (SSH Configuration)
Date: 03/07/2026

## Completed the following activities:

Installed and configured the OpenSSH server.

Started and enabled the SSH service.

Checked SSH service status.

Connected remotely from another Linux system.

Verified successful login using SSH.

Recorded server IP address and login username.


## Commands practiced:

sudo apt install openssh-server

sudo systemctl start ssh

sudo systemctl enable ssh

systemctl status ssh

ssh username@ip_address

hostname -I

whoami

## Task 4 (FTP Configuration) 

# Completed the following activities:

Installed and configured an FTP server.

Created a user account for FTP access.

Connected to the FTP server successfully.

Uploaded a file from the client system.

Downloaded a file from the server.

Verified that file transfer was completed successfully.

## Commands practiced:

sudo apt install vsftpd

sudo systemctl start vsftpd

ftp server_ip

put filename

get filename

Successfully configured FTP and transferred files between two Linux systems
